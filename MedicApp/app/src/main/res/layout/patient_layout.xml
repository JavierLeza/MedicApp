<?xml version="1.0" encoding="utf-8"?>
<!--
    Este linear layout contiene un item individual en la lista de listas que se muestran en ListsActivity
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context="com.medic.medicapp.PatientActivity">


    <!-- TextView muestra el nombre de la lista -->
    <TextView
        android:id="@+id/tv_patient_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/primary_text_light"
        style="@style/TextAppearance.AppCompat.Medium"
        android:padding="16dp"
        android:onClick="listDetail"
        android:clickable="true"/>


    <!-- Linea divisoria entre listas -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray"/>

</LinearLayout>
